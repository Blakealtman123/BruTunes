<h1>All Artists</h1>

<% @artists.each do |artist| %>
	<strong>
		<%= link_to artist.name , artist_path(artist) %>
	</strong>

	<p>
		Likes: <%= artist.likes %>
		<%= link_to 'upvote' , like_artist_path(artist) %>
	</p>
<% end %>


<h3><%= link_to 'Home' , root_path %></h3>